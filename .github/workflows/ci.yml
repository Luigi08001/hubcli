name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        # Use `npm install` rather than `npm ci` because some transitive
        # optional WASM deps (@emnapi/*) resolve differently across OSes —
        # a lock file generated on macOS may not contain the exact versions
        # Linux pulls at install time. `npm install` is more forgiving
        # while still respecting the lock for main deps.
        run: npm install --no-audit --prefer-offline

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Audit
        run: npm audit --audit-level=moderate
        continue-on-error: true

      - name: Checksums
        run: npm run release:checksums && shasum -a 256 -c release/checksums.sha256
